// user enters restaurant name and zip

//user clicks submit
function handleSubmit(){
	//get the zipcode from the zipcode input

	//get the name of the restaraunt

	//call the health department!

	var data; 
	displayRestaurants(data); 

	$( "#target" ).click(function() {
  alert( "Handler for .click() called." );
});

}

function displayRestaurants(restaurants){
	// format data
	// get jquery reference to the div where to display restaurant
	// inject restaurants into the div

}
// user selects restaurant from the list

function selectRestaurant() {
	// choose restaurant
	// get json to retrive information

}

// last inspection date, violations show

function showViolations() {
	// use get json to select violations
	// format information
	// show violations in html
	// show last inspection date in html
}

// graphic recomends eating at or avoiding restuarant

function showGraphic(){
	// choose graphic based on selection
	// display graphic

}

// reset application for a new search "start a new search"

$('#configreset').click(function(){
            $('#configform')[0].reset();
 });





